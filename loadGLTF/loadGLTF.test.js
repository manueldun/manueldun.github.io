const saludar=require("./loadGLTF.js");
/*
test("should return gltf version 2.0",()=>{
    const gltfObjectPromises = loadGLTF2Web("testCube.gltf");
    const meshObject;
    const meshBuffer;
    Promise.all(gltfObjectPromises).then((values)=>{
        meshObject=values[0];
        meshBuffer=values[1];
        test("should be version 2",()=>{
            expect(meshObject.asset.version).equalTo("2.0");
        });
    });
});
*/
